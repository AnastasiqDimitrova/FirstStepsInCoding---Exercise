using System;

namespace _04.VacationBooksList
{
    internal class Program
    {
        static void Main(string[] args)
        {
            int pagesBook = int.Parse(Console.ReadLine());
            int pagesHour = int.Parse(Console.ReadLine());
            int day = int.Parse(Console.ReadLine());
            int hourZaDays = int.Parse(Console.ReadLine());
            int hourTotal = pagesBook / pagesHour;
            int hourZaDay = hourTotal / dey;

            Console.WriteLine(hourDays);
        }
    }
}
